#! /usr/bin/python
import sys
import math

def f(n):

    p=arcsen(n)
    return p

if __name__ == "__main__":

 if (len(sys.argv) != 1):
    n= int(raw_input('Introduzca el valor n: '))

 else:
    n = int(sys.argv[1])

lista=[]
  for i in range (1, n+1):
     p=f(n)
     lista.append(p)
 
  print lista